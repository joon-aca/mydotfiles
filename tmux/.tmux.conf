set -g history-limit 20000

# Disable mouse entirely
set -g mouse off

# Minimal ESC buffering delay
set -sg escape-time 10

# Don't use copy mode
setw -g mode-keys none

# No status redraw overhead
set -g status off

# Don't wait for repeat commands
set -g repeat-time 0
